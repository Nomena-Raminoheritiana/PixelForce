{% extends 'base.html.twig' %}
{% block title %}Coach - Liste Formations Video {% endblock %}
{% block body %}
    <div>
        <div class="row mt-4">
            <div class="col-12 col-md-6">
                <h3>Liste des formations</h3>
            </div>
            <div class="col-12 col-md-6 text-end">
                <a href="{{ path('coach_formation_add') }}" class="btn bg-princ text-white"><i class="fa fa-plus"></i> Ajouter</a>
            </div>
        </div>

        <div class="row">
            <div class="col-12 col-md-2 mt-4">
                <input type="text" class="form-control" placeholder="Titre"/>
            </div>
            <div class="col-12 col-md-2 mt-4">
                <input type="text" class="form-control" placeholder="Déscription"/>
            </div>
            <div class="col-12 col-md-2 mt-4">
                <input type="text" class="form-control" placeholder="Nom de l'auteur"/>
            </div>
            <div class="col-12 col-md-3 mt-4">
                <select class="form-select">
                    <option selected>Etat</option>
                    <option value="all">Tous</option>
                    <option value="disponible">Disponible</option>
                    <option value="brouillon">Brouillon</option>
                </select>
            </div>
            <div class="col-12 col-md-2 mt-4">
                <select class="form-select">
                    <option selected>Ordre/Tri</option>
                    <option value="disponible">ASC</option>
                    <option value="brouillon">DESC</option>
                </select>
            </div>
            <div class="col-12 col-md-1 text-end mt-4">
                <button class="btn bg-princ text-white"><i class="fa fa-search"></i></button>
            </div>
        </div>
    </div>
{% endblock %}