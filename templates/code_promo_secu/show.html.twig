{% extends 'base.html.twig' %}

{% block title %}CodePromoSecu{% endblock %}

{% block body %}
    <h1>CodePromoSecu</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ code_promo_secu.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ code_promo_secu.code }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ code_promo_secu.description }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ code_promo_secu.prix }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ code_promo_secu.statut }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_code_promo_secu_index') }}">back to list</a>

    <a href="{{ path('app_code_promo_secu_edit', {'id': code_promo_secu.id}) }}">edit</a>

    {{ include('code_promo_secu/_delete_form.html.twig') }}
{% endblock %}
